# Rust Tooling

cargo bacon
cargo nextest
(are these compatible with leptos custom build system?)

clippy

- deny warnings
- deny clippy::redundant_clone
- deny clippy::unwrap_used

rustfmt/leptosfmt

rust-toolchain.toml
